import { CardsAnimDirective } from './cards-anim.directive';

describe('CardsAnimDirective', () => {
  it('should create an instance', () => {
    const directive = new CardsAnimDirective();
    expect(directive).toBeTruthy();
  });
});
